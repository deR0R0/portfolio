import { Inter, Figtree, Plus_Jakarta_Sans, Raleway } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
});

export const figtree = Figtree({
    subsets: ['latin'],
});

export const pjs = Plus_Jakarta_Sans({
    subsets: ['latin'],
    display: 'swap',
})

export const raleway = Raleway({
    subsets: ['latin'],
})